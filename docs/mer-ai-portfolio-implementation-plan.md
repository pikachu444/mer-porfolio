# 메르AI 포트폴리오 구현 계획

메르 블로그를 근거로 모델 포트폴리오를 구성하는 시스템의 상세 설계 문서다.
전체 진행 상태와 현재 논의 항목은 [작업 목록](./mer-ai-portfolio-task-list.md)에서 관리한다.

각 항목의 `합의됨`, `논의 필요`, `완료 조건`을 기준으로 구현한다. 합의되지 않은 정책은
임의로 결정하지 않는다.

## 목표

블로거가 실제 매수, 보유, 매도했다고 밝힌 종목을 우선 반영한다. 블로그에서 긍정적으로
분석한 종목이나 섹터를 근거로 AI가 종목 또는 ETF를 제한적으로 추가하여 모델 포트폴리오를
구성한다.

블로거의 직접 판단과 AI의 추론은 반드시 구분하여 표시한다.

## 확인된 현재 문제

- 현재 `기존보유`는 블로거의 실제 보유가 아니라 기존 모델 포트폴리오의 active 상태를 뜻한다.
- 현재 검증은 종목명, 티커 또는 코드가 원문에 등장하는지만 확인한다.
- 종목 주변의 `보유`, `매수`, `편입` 단어만으로 직접 판단을 분류하여 문맥 오인 가능성이 있다.
- AI가 섹터 분석만으로 추가한 종목과 블로거가 직접 판단한 종목이 화면에서 구분되지 않는다.
- 비중 변경 근거와 판단 날짜가 충분히 기록되지 않는다.

## 상세 계획

### 1. 포트폴리오 판단 데이터 구조 정의

**목적:** 블로거의 직접 판단과 AI의 추론을 상태 파일에서 구별한다.

**합의됨**

출처와 행동을 하나의 값으로 합치지 않고 별도 필드로 관리한다.

| 필드 | 허용값 |
|---|---|
| `판단 주체` | `메르`, `AI` |
| `판단` | `매수`, `보유`, `비중확대`, `비중축소`, `매도`, `관망` |
| `근거` | `직접 발언`, `종목 분석`, `섹터 분석`, `이전 판단 유지` |

화면에는 `메르 · 매수`, `AI · 보유`, `AI · 매도 제안`처럼 표시한다.

**논의 필요**

- 내부 JSON 필드명을 확정한다.
- `관망` 종목을 포트폴리오와 별도 목록으로 관리할지 결정한다.

**완료 조건**

- 상태 파일 스키마가 문서에 확정되어 있다.
- 신규 스키마 파싱 테스트가 있다.

### 2. `메르` 직접 판단 인정 기준 정의

**목적:** 블로거 본인의 행동과 글에 등장한 일반 사례를 구별한다.

**합의됨**

- `메르` 판단에는 근거 글 제목, URL, 게시일이 반드시 필요하다.
- 새 글에서 다시 언급되지 않았다면 메르 판단일을 임의로 갱신하지 않는다.
- "리커전이 보유한 데이터" 같은 문장을 블로거의 주식 보유로 분류하면 안 된다.

**논의 필요**

- 어떤 문장을 `메르 · 매수`, `메르 · 보유`, `메르 · 매도`로 인정할지 정한다.
- 직접 비중 발언을 어떤 형식으로 저장할지 정한다.
- 직접 판단을 코드 규칙으로 검증할지, LLM 판정 후 코드가 필수 필드만 검증할지 정한다.

**완료 조건**

- 행동별 인정 및 제외 예시가 문서화되어 있다.
- 오분류 방지 테스트가 있다.

### 3. `AI · 매수` 허용 조건 정의

**목적:** 블로그를 근거로 AI가 모델 포트폴리오를 확장할 수 있는 범위를 정한다.

**합의됨**

- 글에 등장한 종목을 AI가 제안할 수 있다.
- 긍정 섹터를 근거로 AI가 종목 또는 ETF를 제한적으로 제안할 수 있다.
- AI의 제안을 블로거의 직접 추천처럼 표시하면 안 된다.
- 원문에 종목이 등장하지 않았다면 그 사실을 표시한다.

**논의 필요**

- 섹터 분석만 있는 경우 개별 종목, ETF 또는 둘 다 허용할지 정한다.
- 글에 등장한 종목을 단순 사례와 투자 후보로 구별하는 기준을 정한다.
- AI 제안 종목에 사람의 승인 절차가 필요한지 정한다.

**완료 조건**

- 허용 및 차단 규칙이 확정되어 있다.
- 원문 직접 등장 여부가 상태 파일과 보고서에 표시된다.

### 4. 기존 포지션 유지 및 종료 규칙 정의

**목적:** 종목이 새 글에서 사라졌을 때 임의 제거를 방지한다.

**합의됨**

- 새 글에 다시 나오지 않았다는 이유만으로 종목을 자동 제거하지 않는다.
- `메르 · 매도`는 현재 포트폴리오에서 제외하고 종료 이력을 보존한다.
- `AI · 매도`는 자동 제외하지 않고 우선 검토 대상으로 표시한다.

**논의 필요**

- AI 포지션을 언제까지 유지할지 정한다.
- 장기간 근거 갱신이 없는 종목을 별도로 표시할지 정한다.
- AI 매도 제안을 사람이 어떻게 확정할지 정한다.

**완료 조건**

- 유지, 검토, 종료 상태 전이가 구현되어 있다.
- 상태 전이 테스트가 있다.

### 5. 비중 제안 원칙 정의

**목적:** 블로그 근거 없이 목표 비중이 흔들리는 문제를 막는다.

**합의됨**

- 블로거가 직접 비중을 제시했다면 별도 필드에 보존한다.
- 새로운 근거가 없으면 기존 판단과 기존 비중을 임의로 바꾸지 않는다.
- 비중 변경 시 이전 비중, 제안 비중, 변경 이유, 근거 글을 기록한다.

**폐기한 임시값**

아래 숫자는 블로그 분석이나 사용자 합의에서 도출되지 않았으므로 정책으로 사용하지 않는다.

| 항목 | 폐기한 값 |
|---|---:|
| 단일 종목 최대 | 15% |
| 신규 종목 최대 | 10% |
| 단순 직접언급 신규 종목 | 3~5% |
| 현금 최소 | 10% |

**논의 필요**

- 메르 직접 비중이 없는 경우 AI가 어떤 기준으로 비중을 제안할지 정한다.
- 비중 변경 폭 제한이 필요한지 정한다.
- 제한을 자동 보정으로 적용할지 경고만 표시할지 정한다.

**완료 조건**

- Gemini 프롬프트에 반영할 비중 원칙이 확정되어 있다.
- 비중 변경 이유 누락을 감지하는 테스트가 있다.

### 6. Gemini 요청 프롬프트 개편

**목적:** LLM이 출처, 판단, 날짜, 근거 글, 비중 변경 이유를 구조적으로 출력하게 한다.

**합의됨**

Gemini 응답은 최소한 다음 정보를 포함해야 한다.

| 필드 |
|---|
| 종목 |
| 판단 주체 |
| 판단 |
| 판단일 |
| 이전 비중 |
| 제안 비중 |
| 메르 제시 비중 |
| 근거 |
| 원문 종목 등장 여부 |
| 근거 글 제목 및 URL |
| 변경 이유 |

**논의 필요**

- 마크다운 표를 유지할지 구조화된 JSON 응답을 사용할지 정한다.
- 판단 근거를 하나의 글만 저장할지 복수 글을 저장할지 정한다.

**완료 조건**

- `system_prompt.py`가 확정된 스키마를 요청한다.
- 샘플 응답 파싱 테스트가 있다.

### 7. 추천 검증 로직 개편

**목적:** `portfolio_validation.py`가 신규 분류 체계를 검증하게 한다.

**합의됨**

- `메르` 판단은 근거 글, URL, 판단일, 직접 발언 근거가 있어야 한다.
- 원문에 없는 AI 종목도 합의된 조건에 따라 허용할 수 있다.
- 허용된 경우 `AI`, `섹터 분석`, `원문 종목 등장 없음`을 표시한다.
- 명시적 근거가 없는 종목 제거를 차단한다.
- 근거 없는 비중 변경을 경고한다.

**논의 필요**

- 코드가 직접 발언의 진위를 어느 수준까지 자동 검증할지 정한다.
- 잘못된 LLM 출력을 차단할지, 경고 후 유지할지 정한다.

**완료 조건**

- 신규 검증 규칙이 구현되어 있다.
- 정상, 차단, 경고 사례 테스트가 있다.

### 8. 포트폴리오 상태 및 이력 마이그레이션

**목적:** 기존 상태 파일을 신규 판단 구조로 전환한다.

**합의됨**

- 과거 판단을 삭제하지 않는다.
- 현재 포트폴리오에서 제외된 종목은 종료 이력으로 보존한다.
- 기존 `기존보유` 값은 메르 직접 보유로 간주하지 않는다.

**논의 필요**

- 과거 종목을 자동 분류할지 `미분류`로 둘지 정한다.
- 현재 상태에 잘못 편입된 종목을 어떻게 재검토할지 정한다.

**완료 조건**

- 마이그레이션 함수와 테스트가 있다.
- 기존 출력 파일을 읽어도 오류가 발생하지 않는다.

### 9. HTML 보고서 개편

**목적:** 사용자가 블로거의 직접 판단과 AI의 해석을 화면에서 즉시 구별하게 한다.

**합의됨**

HTML에는 다음 내용을 표시한다.

- 현재 모델 포트폴리오
- 신규 편입
- 비중 확대 및 축소
- 매도 및 종료 포지션
- 판단 주체와 판단
- 판단일
- 근거 글 링크
- 변경 이유
- 메르 제시 비중과 AI 제안 비중

**논의 필요**

- 현재 포트폴리오 표에 항상 표시할 컬럼을 정한다.
- 상세 근거를 펼침 영역으로 분리할지 정한다.

**완료 조건**

- 데스크톱 및 모바일 HTML에서 신규 정보가 표시된다.
- 기존 성과 그래프와 표가 정상 동작한다.

### 10. 성과 추적 기준 개편

**목적:** 모델 포트폴리오의 편입 및 종료 성과를 일관되게 추적한다.

**합의됨**

- 동일 종목의 추천일별 중복 성과 행을 만들지 않는다.
- 종료 포지션은 삭제하지 않는다.
- 편입일, 종료일, 종료 근거, 수익률을 보존한다.

**논의 필요**

- 메르 판단 기반과 AI 판단 기반 성과를 분리해서 보여줄지 정한다.
- 비중 변경 시 성과 계산 기준을 어떻게 처리할지 정한다.

**완료 조건**

- 성과 계산 규칙이 문서화되어 있다.
- 중복, 비중 변경, 종료 테스트가 있다.

### 11. Actions 실행 모드 정리

**목적:** 검증 실행이 실제 포트폴리오를 의도치 않게 변경하지 않도록 한다.

**현재 상태**

| 모드 | 역할 |
|---|---|
| `scheduled` | 신규 글 기반 모니터링 |
| `adhoc` | 최근 14일 글 기반 전면 재검토 및 상태 저장 |
| `test` | 상태 저장과 Telegram 전송 없이 전면 재검토 |

**논의 필요**

- `adhoc` 이름을 `rebalance`로 변경할지 정한다.
- 상태를 변경하지 않고 HTML과 Telegram 형식만 검증하는 `verify` 모드를 추가할지 정한다.

**완료 조건**

- 모드 이름과 동작이 명확하다.
- 모드별 상태 저장 여부 테스트가 있다.

### 12. 통합 검증 및 배포

**목적:** 변경된 전체 흐름을 실제 환경에서 확인한다.

**검증 범위**

- Gemini 응답 파싱
- 추천 검증
- 상태 파일 갱신
- 성과 추적
- HTML 및 PNG 생성
- Telegram 출력
- GitHub Actions 실행

**완료 조건**

- 자동 테스트가 통과한다.
- 상태를 훼손하지 않는 방식으로 실제 Actions 결과를 확인한다.
- 검증 결과를 문서에 기록한다.

## 기존 완료 작업

- [x] 동일 종목의 추천일별 중복 성과 행 제거
- [x] 현재 포트폴리오와 종료 포지션 분리
- [x] 새 글에 종목이 없다는 이유만으로 기존 항목을 자동 제거하지 않도록 수정
- [x] 신규 종목의 원문 등장 여부 또는 기존 active 여부를 확인하는 1차 검증
- [x] 1차 검증에서 제외된 신규 종목의 비중을 현금으로 이동
- [x] HTML에 현재 포트폴리오, 매수 및 변경, 매도 및 제외 표 추가

현재의 1차 검증은 블로거의 직접 판단을 검증하는 기능이 아니다. 위 작업 목록에 따라
교체해야 한다.
