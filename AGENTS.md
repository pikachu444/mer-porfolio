# 메르AI 포트폴리오 개발 지침

## 기준 문서

이 저장소의 모델 포트폴리오 개선 작업은 아래 두 문서를 기준으로 진행한다.

1. [작업 목록](./docs/mer-ai-portfolio-task-list.md)
2. [구현 계획](./docs/mer-ai-portfolio-implementation-plan.md)

작업 목록은 진행 상태의 기준이고, 구현 계획은 정책과 완료 조건의 기준이다.

## 작업 절차

### 새 세션 시작

새 세션에서 이 저장소의 작업을 시작하면 코드 수정 전에 반드시 다음 순서로 현재 상태를
확인한다.

1. 이 파일 `AGENTS.md`를 읽는다.
2. [작업 목록](./docs/mer-ai-portfolio-task-list.md)을 읽고 현재 논의 또는 구현 항목을 확인한다.
3. [구현 계획](./docs/mer-ai-portfolio-implementation-plan.md)에서 해당 번호의 `합의됨`,
   `논의 필요`, `완료 조건`을 읽는다.
4. `git status --short --branch`와 최근 커밋을 확인한다.
5. 작업과 관련된 기존 코드를 읽고 문서와 구현 상태가 일치하는지 확인한다.
6. `논의 필요`가 남아 있으면 임의로 구현하지 않고 사용자와 먼저 논의한다.

### 논의 및 구현

1. 사용자와 정책을 합의하면 같은 작업 흐름 안에서 구현 계획의 해당 항목을 즉시 갱신한다.
2. 현재 논의 항목이 바뀌면 작업 목록의 `현재 논의`와 체크 상태를 즉시 갱신한다.
3. 구현을 시작하면 작업 목록의 해당 항목을 `[~]`로 표시한다.
4. 구현, 테스트, 문서 갱신이 모두 끝난 경우에만 `[x]`로 표시한다.
5. 새 요구사항이나 문제가 발견되면 기존 항목에 억지로 포함하지 말고 작업 목록에 추가한다.

### 대화형 기획

사용자가 정책을 논의하는 동안에는 Default mode에서도 아래 규칙을 따른다.

1. 한 번에 작업 목록의 현재 논의 항목 하나만 다룬다.
2. 사용자가 명시적으로 구현을 요청하기 전에는 소스 코드를 수정하지 않는다.
3. 질문이 필요한 경우 현재 항목을 결정하는 데 필요한 질문만 한다.
4. 사용자가 합의한 내용만 구현 계획에 즉시 기록한다.
5. 합의하지 않은 내용을 관행, 일반론 또는 임의의 숫자로 채우지 않는다.
6. 현재 항목의 논의가 끝나면 다음 항목으로 이동하기 전에 사용자에게 확인한다.
7. 문서 편집은 기획 작업의 일부이므로 소스 코드 수정 금지와 별개로 계속 수행한다.

### 문서 재확인

아래 시점에는 관련 문서를 다시 읽고 현재 상태를 확인한다.

- 새 세션 시작 시: `AGENTS.md`, 작업 목록, 구현 계획 확인
- `AGENTS.md` 수정 후: `AGENTS.md` 다시 확인
- 현재 논의 항목 이동 후: 작업 목록과 구현 계획의 해당 항목 다시 확인
- 구현 시작 전: 작업 목록, 구현 계획, `git status`, 최근 커밋 다시 확인
- 사용자 요청과 문서 내용이 충돌할 때: 최신 사용자 요청을 기준으로 문서 갱신

### 문서 자동 갱신

문서 갱신은 선택 사항이 아니라 작업의 일부다. 사용자가 별도로 요청하지 않아도 다음 변경을
자동으로 반영한다.

- 정책 합의 또는 변경: 구현 계획의 해당 `합의됨`, `논의 필요` 갱신
- 현재 논의 이동: 작업 목록의 `현재 논의`와 `[ ]`, `[~]`, `[x]` 상태 갱신
- 새 개발 항목 발견: 작업 목록에 번호를 추가하고 구현 계획에 상세 항목 추가
- 구현 완료: 완료 조건 충족 여부, 테스트 결과, 관련 코드 변경 사항 기록
- 기존 계획 폐기: 삭제하지 말고 폐기 사유를 남겨 이후 세션이 다시 제안하지 않게 함

## 도메인 원칙

- 이 시스템은 일반적인 퀀트 포트폴리오가 아니다.
- 메르 블로그의 직접 판단과 AI의 추론을 구분한다.
- AI의 추론을 메르의 직접 추천처럼 표시하지 않는다.
- 기존 모델 포트폴리오 편입과 메르의 실제 보유를 혼동하지 않는다.
- 근거가 없는 비중, 상한, 현금 비율을 임의로 정책에 추가하지 않는다.
- 새로운 근거가 없다면 판단과 비중을 임의로 변경하지 않는다.
- 종목 변경은 근거 글, URL, 판단일을 추적할 수 있어야 한다.

## 구현 품질 원칙

- 모든 기능은 fallback에 의존하지 않고 정상 경로가 안정적으로 동작하도록 설계한다.
- 예외 처리는 필요한 경우에만 두며, 불완전한 결과를 조용히 통과시키는 수단으로 사용하지 않는다.
- 실패 가능성을 당연하게 전제하지 않는다. 입력, 상태 전이, 출력 흐름을 충분히 검토하고 테스트한다.
- 부분 성공으로 내부 상태와 사용자에게 보이는 결과가 어긋나지 않도록 한다.
- 구현 후에는 관련 테스트와 실제 실행 경로를 확인한다.

## 변경 관리

- 문서와 코드가 충돌하면 구현 계획을 먼저 검토한다.
- 구현 계획과 사용자 요청이 충돌하면 최신 사용자 요청을 반영하고 문서를 함께 갱신한다.
- 과거 상태 파일과 출력 결과를 삭제하거나 복원하기 전에 사용자에게 영향을 설명하고 확인받는다.
